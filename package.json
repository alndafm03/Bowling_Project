{
  "name": "bowling-physics-sim",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "A physically-accurate 3D bowling simulation with a custom-built rigid-body physics engine (NO external physics library) — skid → hook → roll phases, differential lane friction, impulse-based collisions. Rendering by Three.js only.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.161.0"
  },
  "devDependencies": {
    "vite": "^5.1.6"
  }
}
