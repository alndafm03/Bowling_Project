# دليل الفريق — محاكي البولينج الفيزيائي

> **للزملاء:** هذا الملف يعطيك خريطة كاملة للمشروع حتى تعرف أين تعدّل وماذا تلمس.  
> النسخة الحية على `npm run dev` ← `http://localhost:5173`

---

## ما هو المشروع؟

محاكاة ثلاثية الأبعاد لكرة البولينج، فيها **محرك فيزياء مكتوب من الصفر** (بدون مكتبة cannon/ammo/rapier).  
المكتبة الوحيدة الخارجية هي **Three.js** وتُستخدم فقط للرسم (Rendering) والحساب الرياضي للمتجهات.

---

## هيكل المجلدات — ماذا يوجد أين؟

```
src/
├── config.js                ← ⭐ كل الأرقام الفيزيائية هنا (ابدأ من هنا دائماً)
├── main.js                  ← نقطة دخول التطبيق، يبني كل الأنظمة ويشغّل الحلقة
├── SimulationController.js  ← قلب اللعبة: آلة الحالات، كشف الفاز، التسجيل
│
├── physics/                 ← محرك الفيزياء المخصص
│   ├── PhysicsEngine.js     ← الوالد: يُقدّم الزمن ويُنسّق كل شيء
│   ├── RigidBody.js         ← الجسم الصلب: موضع، سرعة، عزم القصور، نبضات
│   ├── Integrator.js        ← معادلات التكامل (Euler شبه الضمني)
│   └── BowlingPhysicsModel.js ← المعادلات التحليلية من الدراسة (للعرض في HUD)
│
├── collision/               ← نظام التصادم
│   ├── CollisionSystem.js   ← الطور العريض (AABB) + الطور الضيق + المحلّل
│   └── Shapes.js            ← اختبارات كرة/كبسولة + قيد Contact
│
├── entities/                ← الكيانات المرئية+الفيزيائية
│   ├── Ball.js              ← الكرة (mesh + RigidBody + مُصادِمات)
│   ├── Pin.js               ← دبوس واحد
│   ├── PinSet.js            ← الـ10 دبابيس معاً
│   └── Lane.js              ← الممر والجوانب والحواف
│
├── rendering/               ← كل ما يخص الشاشة
│   ├── Renderer.js          ← WebGLRenderer، المشهد، الإضاءة
│   ├── CameraManager.js     ← 6 أوضاع كاميرا + مستمع صوت
│   └── DebugDraw.js         ← رسم ناقلات الفيزياء (وضع Debug)
│
├── audio/AudioManager.js    ← أصوات 3D مولّدة إجرائياً (بدون ملفات صوت)
├── ui/HUD.js                ← لوحة البيانات الحية (سرعة، طاقة، فاز...)
├── ui/UIControls.js         ← أشرطة التحكم والأزرار والمفاتيح
└── utils/Geometry.js        ← حسابات هندسية (أقرب نقطة، AABB)
```

---

## أين تُعدّل ماذا؟

### تغيير الأرقام الفيزيائية (أهم شيء)
**الملف:** `src/config.js` — كل الثوابت في مكان واحد.

| أريد تعديل | اذهب إلى في config.js |
|---|---|
| سرعة إطلاق الكرة الافتراضية | `ball.startZ` / الـ Slider في UIControls |
| احتكاك الممر | `friction.oil` / `friction.dry` / `friction.transition` |
| معامل الارتداد (كيف تطير الدبابيس) | `restitution.ballPin` / `restitution.pinPin` |
| كتلة الكرة / نصف قطرها | `ball.mass` / `ball.radius` |
| كتلة الدبوس / حجمه | `pin.mass` / `pin.height` / `pin.capsuleRadius` |
| دقة المحاكاة | `sim.fixedTimeStep` / `sim.solverIterations` |
| مقاومة الهواء | `drag.Cd` / `drag.rhoAir` |

### تغيير المظهر المرئي
**الملف:** `src/rendering/Renderer.js` ← إضاءة، ألوان، ظلال  
**الملف:** `src/entities/Ball.js` / `Pin.js` / `Lane.js` ← شكل الكيانات وألوانها

### تغيير واجهة المستخدم
**الملف:** `src/ui/UIControls.js` ← أشرطة التمرير والأزرار  
**الملف:** `src/ui/HUD.js` ← لوحة البيانات اليمنى  
**الملف:** `src/style.css` ← تنسيق CSS

### تغيير الكاميرا (إضافة زاوية جديدة)
**الملف:** `src/rendering/CameraManager.js` ← أضف وضعاً جديداً في مصفوفة `MODES`

### تغيير نظام الصوت
**الملف:** `src/audio/AudioManager.js`

---

## تدفق البرنامج خطوة بخطوة

```
1. main.js يُنشئ كل الأنظمة → يمررها إلى SimulationController
2. المستخدم يضغط LAUNCH → SimulationController.launch()
3. كل إطار (frame):
   a. engine.step(dt)          ← الفيزياء (خطوات ثابتة Δt = 1/120 ث)
   b. ball.sync() / pins.sync() ← مزامنة الـ mesh مع الجسم الفيزيائي
   c. camera.update()           ← تحديث الكاميرا
   d. audio._handleAudio()      ← أصوات الاصطدام
   e. hud.setLive()             ← تحديث لوحة البيانات
   f. renderer.render()         ← رسم الإطار
```

---

## آلة الحالات (phases) — متى يكون الوضع ماذا؟

| الوضع | متى |
|---|---|
| **Ready** | قبل الإطلاق |
| **Fall** | الكرة في الهواء بعد الإطلاق |
| **Skid** | انزلاق كبير في منطقة الزيت (احتكاك منخفض) |
| **Hook** | انحراف جانبي في المنطقة الجافة (احتكاك عالٍ + دوران جانبي) |
| **Roll** | الكرة تتدحرج نظيفاً (v ≈ ω·R) |
| **Collision** | لحظة ضرب الكرة لدبوس |
| **Scatter** | الدبابيس لا تزال تتحرك |

كود الكشف: `SimulationController.classifyPhase()` في `SimulationController.js`

---

## قيم مفيدة للاختبار السريع

```js
// في config.js — جرّب هذه القيم لاختبار سيناريوهات مختلفة:

// كرة سريعة جداً:
ball: { ...، /* اضبط V₀ من الـ slider إلى 12 m/s */ }

// ممر جاف تماماً (انحراف مبكر):
friction: { oil: 0.12, dry: 0.15, ... }

// ممر زيتي (انزلاق طويل بدون انحراف):
friction: { oil: 0.01, dry: 0.06, ... }

// دبابيس أخف (تطير أكثر):
pin: { mass: 0.8, ... }

// محاكاة أدق (أبطأ):
sim: { fixedTimeStep: 1/240, solverIterations: 20, ... }
```

---

## مفاتيح التحكم الكاملة

| المفتاح | الوظيفة |
|---|---|
| `Space` | إطلاق الكرة |
| `R` | إعادة الضبط |
| `C` | تدوير وضع الكاميرا |
| `1` – `6` | أوضاع كاميرا مباشرة (Orbit / Follow / Top / Side / Impact / Free) |
| `B` | تفعيل/إيقاف الحواجب الجانبية |
| `G` | وضع Debug (ناقلات الفيزياء) |
| `M` | كتم الصوت |

---

## ملاحظات مهمة للتعديل

1. **لا تلمس `Integrator.js` إلا إذا فهمت Symplectic Euler** — الخطأ فيه يكسر كل الفيزياء.
2. **`config.js` هو المكان الوحيد للأرقام** — لا تضع ثوابت فيزيائية مباشرة في الكود.
3. **`RigidBody.applyImpulse`** هو الأساس الذي يبني عليه كل شيء — تعديله يؤثر على كل تصادم.
4. **`CollisionSystem.solve`** يحل النبضات عدة مرات (`solverIterations`) — زيادة العدد = دقة أعلى وأبطأ.
5. **Three.js للرسم فقط** — أي `THREE.Vector3` في ملفات الفيزياء هو مجرد حساب رياضي، ليس رسماً.

---

## تشغيل المشروع

```bash
npm install       # مرة واحدة فقط
npm run dev       # تطوير محلي على localhost:5173
npm run build     # بناء للإنتاج → مجلد dist/
npm run preview   # معاينة البناء
```

**متطلب:** Node.js 18 أو أحدث.
